global:
  topology: "SqlFilter"
  bundleTopicName: "bundle-sqlfilter-dev"
  image:
    registry: localhost:5000
    tag: latest

processorGroups:
  - eventRange:
      begin: 0
      end: 3
    subscribers:
      - subscriber
      - subscriber
      - subscriber
